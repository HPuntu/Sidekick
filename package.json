{
  "name": "obsidian-agent-dashboard",
  "version": "0.1.0",
  "description": "An Obsidian dashboard surface for local agentic coding workflows.",
  "main": "main.js",
  "scripts": {
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "release:check": "npm run typecheck && npm run build && npm audit --omit=dev",
    "release:zip": "npm run release:check && mkdir -p dist && zip -j dist/agent-dashboard-0.1.0.zip main.js manifest.json styles.css",
    "typecheck": "tsc -noEmit -skipLibCheck"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "agent",
    "ollama",
    "dashboard"
  ],
  "author": "Agent Dashboard contributors",
  "license": "MIT",
  "files": [
    "main.js",
    "manifest.json",
    "styles.css"
  ],
  "devDependencies": {
    "@types/node": "^20.11.30",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.28.0",
    "obsidian": "^1.5.12",
    "tslib": "^2.6.2",
    "typescript": "^5.4.5"
  }
}
